# 我给自己的视频频道做了一套 AI 后期系统

这套工具叫 [lizheng-video-production](https://github.com/sunyuzheng/lizheng-video-production)。它从视频或已有字幕开始，完成本地转写、全文精校、重新断句、VTT/QC，并按需生成高光、文章、标题和 YouTube description。访谈还可以追加说话人归因；口头禅剪辑、封面、独立录音和社区回放则作为明确的条件工作流接入。

它首先服务「课代表立正」，因此带有频道自己的术语、标题样本和编辑判断。但更重要的并不是这几份 prompt，而是我在长期使用里逐渐想清楚的几条系统原则。

## 不是所有环节都应该交给模型

字幕时间码、文件格式、断句、输出路径和质量检查应该尽量确定、可测试。模型适合处理语境、实体一致性和候选生成，却不应该决定一个 SRT 有没有重叠，也不应该在失败后悄悄留下一个看似可交付的 VTT。

因此系统把 QC 设成依赖门：字幕不通过，下游内容不继续。文件存在也不等于运行成功，因为它可能是上一次留下的旧产物。

反过来，高光、标题和封面不能靠越来越长的规则替代编辑判断。一个片段值得放在开头，可能因为第一手信息、人物魅力、解释能力、悬念或它在整期叙事里的位置。代码可以把候选、时间戳和材料准备好，最后的轻重仍然需要人理解内容。

## 在信息缺失发生的位置补信息

第一次出现的嘉宾名如果在 ASR 阶段被听成同音字，下游模型很难凭空知道正确写法。`--seeds` 在转写前注入本期人名、公司和产品；精校再做全文一致性检查。

这比维护一个不断膨胀的全局替换词典更可靠。单期实体属于当期上下文，只有跨期复用且确认过的术语才进入频道词库。

## 好文章不应该被视频 pipeline 写成平均答案

访谈和单口需要不同的编辑判断，社区帖、独立文章和视频伴读也不是同一种产物。系统因此把视频流程与写作 owner 分开：访谈路由到 `expert-interview-article`，单口路由到 `substance-writing-review`；视频脚本只负责组织逐字稿、高光、嘉宾资料、采访者观察和发布 surface。

每次运行会保存实际采用的 writing skill 主文件、来源和 hash。这样以后至少能核对或固定当时的写作契约，而不是依赖一次不可见的会话状态；要完整复现文章，还需要相同代码版本和本期素材。

## 历史高播放标题是证据，不是牢笼

标题流程先辨认核心观众在这个题材上原本就有的观看动机，再从完整材料里寻找能改变理解的事实、数字、冲突或人物关系。动机可以来自自己的判断，也可以来自人物或事件本身已有的意义；系统不再替一个远距离传奇事后发明用途。标题与封面从第一轮就是组合；独立 challenger 会在看见首轮答案之前重读源材料并另做候选，再把两套方案放进陌生信息流冷测。频道真实高播放标题仍是外部参照，只用于校准观众曾为什么付出注意力，不替代当期判断。

但真实样本的用途是发现盲区，不是宣布以后只能写同一种标题。用户要快速 brainstorm 时，编辑当然可以直接写；新平台、新受众和新内容也可能需要历史样本里没有的表达。

真正值得积累的是机制：为什么这次大家不知道嘉宾厉害在哪里？为什么一个专业名词没有给大众观众任何识别？为什么标题有观点却没有 stakes？理解这些以后，系统会提高判断力，而不是只增长“必须”和“不能”的清单。

## 文件比会话记忆可靠

源媒体不覆盖，raw ASR、corrected 和 final 各自有明确角色；本次 ASR 失败也不会破坏上一份 raw。中间状态放进 `<video>_process/`，最终交付留在视频同目录。采访者回看后的观察也写进本期文件，作为待验证假设交给文章阶段，而不是假设模型还记得上一次聊天。

这看起来是一个工程细节，其实决定了 AI 工作流能不能被复核、重跑和交给别人。

## 它真正自动化什么

仓库把能力分成四层：主脚本自动完成的核心链路、可单独运行的增强脚本、外部 skill／connector，以及 agent 根据 recipe 完成的制作工作。双录音、平台上传或封面设计不会因为 README 写了步骤就被冒充成“已经自动化”。

一个命令可以跑核心链路：

```bash
caffeinate -i venv/bin/python tools/process_video.py video.mp4 \
  --seeds 嘉宾名 公司名 产品名
```

具体安装、产物和限制见仓库 [README](https://github.com/sunyuzheng/lizheng-video-production#readme)。如果把它 fork 给自己的频道，最值得替换的不是某个神奇 prompt，而是你的术语、真实标题样本，以及你对好内容为什么好的解释。
